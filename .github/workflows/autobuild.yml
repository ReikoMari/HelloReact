on: push

jobs:
  npm-build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: 
        npm install
        npm run build
    - name: githubpage
      uses: JamesIves/github-pages-deploy-action@v4
      with:
        branch: gh-page
        folder: build
